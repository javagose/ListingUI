export interface TableParams {

    windowWidth : string;
    uri : string;
    params : string;
    reorderableColumns : boolean;
    resizableColumns : boolean ; 
    responsive : boolean;
    scrollable : boolean; 
    paginator : boolean; 
    rows : number; 
    editable : boolean;
}